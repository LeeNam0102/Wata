@using Wata.Commerce.Sample.Module.Models.Abc
@inject IAbcFilters Filters

@if (Column == Filters.SortColumn)
{
    <span>@SortSymbol</span>
}
else
{
    <span>&nbsp;</span>
}
@code {
    /// <summary>
    /// Sets which <see cref="AbcFilterColumns"/> this indicator is for.
    /// </summary>
    [Parameter]
    public AbcFilterColumns Column { get; set; }

    /// <summary>
    /// The symbol to render.
    /// </summary>
    private string SortSymbol => Filters.SortDescending ? "🔼" : "🔽";
}