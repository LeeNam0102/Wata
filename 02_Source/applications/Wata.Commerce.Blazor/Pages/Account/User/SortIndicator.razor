@using Wata.Commerce.Account.Module.Models.User
@inject IUserFilters Filters

@if (Column == Filters.SortColumn)
{
    <span>@SortSymbol</span>
}
else
{
    <span>&nbsp;</span>
}
@code {
    /// <summary>
    /// Sets which <see cref="UserFilterColumns"/> this indicator is for.
    /// </summary>
    [Parameter]
    public UserFilterColumns Column { get; set; }

    /// <summary>
    /// The symbol to render.
    /// </summary>
    private string SortSymbol => Filters.SortDescending ? "🔼" : "🔽";
}