@using Wata.Commerce.Account.Module.Models.Role
@inject IRoleFilters Filters

@if (Column == Filters.SortColumn)
{
    <span>@SortSymbol</span>
}
else
{
    <span>&nbsp;</span>
}
@code {
    /// <summary>
    /// Sets which <see cref="RoleFilterColumns"/> this indicator is for.
    /// </summary>
    [Parameter]
    public RoleFilterColumns Column { get; set; }

    /// <summary>
    /// The symbol to render.
    /// </summary>
    private string SortSymbol => Filters.SortDescending ? "🔼" : "🔽";
}